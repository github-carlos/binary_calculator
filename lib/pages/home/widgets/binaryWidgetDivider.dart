import 'package:flutter/material.dart';
class BinaryWidgetDivider extends StatelessWidget {
  const BinaryWidgetDivider({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 2,
      color: Colors.black,
    );
  }
}